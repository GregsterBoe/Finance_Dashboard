{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "msedge", // or "chrome"
      "request": "launch",
      "name": "Launch Vite React App",
      "url": "http://localhost:5173",
      "webRoot": "${workspaceFolder}"
    }
  ]
}